import { api, client, init, initialise, resetConfig, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  init,
  initialise,
  resetConfig,
  updateInstanceUrl,
  updateToken
}